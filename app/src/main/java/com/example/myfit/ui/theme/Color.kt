package com.example.myfit.ui.theme

import androidx.compose.ui.graphics.Color

// Brand Palette
val Gray950 = Color(0xFF030712) // app background (darkest)
val Gray900 = Color(0xFF111827) // cards / surfaces / dialogs
val Gray800 = Color(0xFF1F2937) // inputs / secondary buttons / elevated surfaces
val Gray700 = Color(0xFF374151) // default borders / nested inputs / progress track
val Gray600 = Color(0xFF4B5563) // hover borders / disabled icons
val Gray400 = Color(0xFF9CA3AF) // secondary text / field labels
val White = Color(0xFFFFFFFF)   // primary text / primary action button

val Teal400 = Color(0xFF2DD4BF) // links / accents / sign-up
val Cyan500 = Color(0xFF06B6D4) // gradient end (logo, checks, progress)

val Red400 = Color(0xFFF87171) // destructive text / error messages
val Red600 = Color(0xFFDC2626) // destructive buttons
